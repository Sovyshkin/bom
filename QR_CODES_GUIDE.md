# ✅ ИСПРАВЛЕНА ОШИБКА 404 ПРИ СКАНИРОВАНИИ QR КОДОВ

## Проблема
При сканировании QR кода возникала ошибка 404:
```
Request URL: http://localhost:1337/api/api/proekts/9
Status Code: 404 Not Found
```

## Причина
**Дублирование `/api/` в URL**

### Как это происходило:
1. **baseURL в axios.js**: `http://localhost:1337/api`
2. **Запрос в коде**: `axios.get('/api/proekts/9')`
3. **Итоговый URL**: `http://localhost:1337/api/api/proekts/9` ❌

## Исправления

### В файле `WorkerStage.vue`:

**Было:**
```javascript
const projectResponse = await axios.get(`/api/proekts/${projectId}`)
const elementResponse = await axios.get(`/api/element/${elementId}`)
const stageResponse = await axios.get(`/api/etaps/${stageId}`)
```

**Стало:**
```javascript
const projectResponse = await axios.get(`/proekts/${projectId}`)
const elementResponse = await axios.get(`/element/${elementId}`)
const stageResponse = await axios.get(`/etaps/${stageId}`)
```

### Исправлены все API вызовы:
✅ Загрузка проекта: `/proekts/${projectId}`
✅ Загрузка элемента: `/element/${elementId}`  
✅ Загрузка этапа: `/etaps/${stageId}`
✅ Загрузка подэтапа: `/etap-zagotovki/${subStageId}`
✅ Обновление статуса этапа: `/etaps/${id}/status`
✅ Обновление статуса подэтапа: `/etap-zagotovki/${id}/status`

## Результат
🎉 **QR коды теперь работают корректно!**

Правильные URL:
- `http://localhost:1337/api/proekts/9` ✅
- `http://localhost:1337/api/element/14` ✅  
- `http://localhost:1337/api/etaps/156` ✅

---

# ✅ QR КОДЫ - ПОЛНОЕ РУКОВОДСТВО

## Как это работает

1. **Сканирование QR-кода**: Работник сканирует QR-код своей камерой телефона
2. **Переход по ссылке**: Браузер открывает ссылку из QR-кода
3. **Проверка авторизации**: Если пользователь не авторизован, его перенаправляет на страницу входа
4. **Редирект после входа**: После успешной авторизации пользователя автоматически перенаправляет на страницу этапа
5. **Управление работой**: На странице этапа есть кнопки "Приступить к работе" и "Закончить работу"

## Формат QR-ссылок

QR-коды содержат ссылки в формате:
```
https://yourdomain.com/qr/{proektId}/{elementId}/{stageId}
```

Где:
- `proektId` - ID проекта
- `elementId` - ID элемента  
- `stageId` - ID этапа

## Пример ссылок

```
https://localhost:3000/qr/1/25/156
https://localhost:3000/qr/2/78/89
```

## Отслеживание работы

В базе данных сохраняется:
- Кто начал работу (`startedBy` - ссылка на пользователя)
- Когда начал работу (`startedAt` - дата и время)
- Кто завершил работу (`finishedBy` - ссылка на пользователя) 
- Когда завершил работу (`finishedAt` - дата и время)
- Текущий статус (`status` - "Не начат", "В работе", "Готов")

## API endpoints

- `GET /api/etaps/{id}` - получить информацию об этапе
- `PUT /api/etaps/{id}/status` - обновить статус этапа

Пример запроса для начала работы:
```json
PUT /api/etaps/156/status
{
  "status": "В работе",
  "userId": 123
}
```

Пример запроса для завершения работы:
```json  
PUT /api/etaps/156/status
{
  "status": "Готов",
  "userId": 123
}
```

## Генератор QR-кодов

Для создания QR-кодов используйте страницу `/qr-generator` в административной панели.

## Безопасность

- Все страницы этапов требуют авторизации
- Завершить работу может только тот пользователь, который её начал
- Данные о времени работы и исполнителях сохраняются в БД для аудита